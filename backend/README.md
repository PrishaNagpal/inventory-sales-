# Backend (FastAPI)

Owner: REST APIs, auth, transactions (sales/purchases must update stock atomically).

## Planned stack

- FastAPI + Pydantic v2
- SQLAlchemy 2 + PostgreSQL
- JWT auth

API docs will be at `http://localhost:8000/docs` once the app is running.
