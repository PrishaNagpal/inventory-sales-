# Database (PostgreSQL)

Owner: schema, constraints, indexes, views, triggers, seed data.

## Files

- `schema.sql` — tables, FKs, indexes
- `seed.sql` — sample products, suppliers, customers
- Put the ER diagram in `/docs` when ready

Schema is the team contract. Merge table changes here before the backend uses them.
