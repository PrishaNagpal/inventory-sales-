# Setup

## Prerequisites

- Git
- Docker Desktop (for PostgreSQL)
- Python 3.11+ (backend)
- Node.js 20+ (frontend)

## First clone

```bash
git clone <github-url>
cd inventory-sales
copy .env.example .env
docker compose up -d
```

Add teammates as GitHub collaborators after the remote is created.
